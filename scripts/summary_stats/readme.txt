stats_sliding_window.py generates summary statistics per simulation replicate.

get_mean_stats.py will calulate divergence, and get the mean stats for each window across replicates.

Use masked versions to mask exonic sites.
